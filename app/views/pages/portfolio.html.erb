<article class="portfolio">
	<%= render 'posts/post_breadcrumbs', breadcrumbs: @breadcrumbs %>
	<% if @portfolio.icon %>
		<%= image_tag @portfolio.icon, class: "chapterHeading" %>
	<% end %>
	<h2 class='title'><%= @portfolio.name %></h2>
	<div class='portfolioBody'>
		<% @portfolio.posts.each do |post| %>
			<% @post = post %>
			<%= render('posts/portfolio_summary') %>
		<% end %>
	</div>
	<% if logged_in %>
		<p>
			<%= link_to 'Edit Portfolio', edit_portfolio_path(@portfolio) %> <br />
			( Published means appearing on main page. Direct links still accessible.) <br />
			<% if !@portfolio.published %>
				<strong>(NOT PUBLISHED)</strong>
			<% end %>
			<br />
			<%= link_to 'Dashboard', '/admin/dashboard' %>
		</p>
	<% end %>
</article>